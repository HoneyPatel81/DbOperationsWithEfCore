# DbOperationsWithEfCore

hello


make empty repository on GitHub and copy its HTTPS link
open terminal and navigate to the desired folder using 'cd' command
write these commands:
git clone <url that you copied>                     - repository will be cloned to local pc
git add .                           - all files will move to staging area (locally)
git commit -m "your message"        - files will be commited (locally)
git push origin main                - all committed files will be uploaded on github
